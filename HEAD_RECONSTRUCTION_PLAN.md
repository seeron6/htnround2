# Head reconstruction: evidence, ownership and validation

Updated 19 September 2026. This is an implementation and research record, not a claim of hyperrealistic reconstruction from every video. See [OPEN_SOURCE_STACK.md](OPEN_SOURCE_STACK.md) for this machine's dependency and licensing constraints.

## Why the defects occurred

The original pipeline fitted the facial landmarks but retained generic template ears. Projecting a side photograph onto a misplaced ear also projected the real ear onto the adjacent skull. That created two apparent ears. Eyeglasses had the same ownership problem: a separate 3D accessory was added, but imprecise masks and weak side-view samples left parts of the photographed frames on the skin. Broad missing-hair material could also cover ears. Ordinary photographs were projected onto the artificial neck closure.

A second source of error was geometric: `photo_cameras.recover()` could reuse legacy self-calibrated cameras before considering a supplied field of view. Low reprojection error alone does not establish correct focal length or head depth.

Texture transitions also had independent implementation defects. A binary cleanup mask excluded the original camera even at the feather's outer edge; an observed/estimated color threshold switched abruptly between sources; and a mixed ear/skull triangle inherited its first vertex's surface label. The bake now retains feathered camera contributions, blends estimated color continuously, and determines surface ownership per texel using barycentric weights. Cyclically reordering triangle vertices no longer changes that ownership. Cleanup cache validation checks the current annotation-derived crop and mask as well as the source image, and can re-register an existing generated edit without requesting another one.

A traced bright patch at the upper ear had no usable camera sample and inherited a constant cheek-colored fallback. Unsupported ear material now continues nearby photographed color on that ear, within a bounded distance and with compatible surface normals. The opposite ear, scalp and opposite-facing folds cannot donate color. This remains estimated appearance and does not increase measured coverage.

The hair silhouette fit also treated lateral ear vertices as part of its adjustable envelope. It now pins the template ear regions and fades deformation over a 12 mm geodesic attachment margin. A separate ear-fit issue was an unnecessarily coarse step search: one nearly failing triangle reduced the entire deformation from 100% to 75%. Six bounded refinement steps now recover the largest tested safe step without relaxing the orientation, area or intersection limits.

The original landmark test checked the triangulated control cage, which is not the rendered skin. Forty-seven semantic locations are now bound barycentrically to actual surface triangles. A nested split of the geometry-training frames selects the template regularization; reserved outer views do not select that parameter. Candidates must also pass displacement, triangle orientation, area and intersection limits. After the hair, eye-depth and ear stages, the final rendered surface must pass the reserved-view projection check. Those views may still supply texture and completion evidence, so this is not a fully unseen-image appearance benchmark.

A later temple trace found that a verified but dark frontal cleanup reference could override brighter side-view skin. Cleanup now estimates a bounded, smooth color offset from the outer feather in surface space. Nearby compatible normals supply the offset; hidden frame centers, other surface parts and central frontal features do not determine that correction. This reduces exposure seams without treating synthesized skin as measured appearance.

A traced underside failure came from nearest-color continuation itself: its changing donor neighborhoods created angular wedges, and a forward blending gate left some unseen texels gray. Lower-head completion now accumulates reliable photographic colors onto their actual mesh vertices, holds those anchors fixed, solves a positive-weight harmonic field over connected surface edges, and interpolates it into the atlas using the same barycentric bindings as rasterization. Artificial neck-cap edges are excluded, UV seams do not create discontinuities, and disconnected regions without photographic anchors stay unresolved. A later nearest-face fallback runs before this continuation so it cannot overwrite the repaired chin. Confidence, mouth, ear, eye and scalp masks preserve their separate materials. This does not invent measured underside coverage.

Generated eyes now receive bounded short-range visibility shading from the fitted globe, face and lids, applied in linear light. Only generated eye parts are changed; photographic iris materials and skin remain exact. This softens the flat globe appearance but is estimated occlusion, not recovered illumination or verified eye reflectance.

A further jaw trace separated camera support from exposure preference. A grazing frontal sample with facing cosine 0.503 had physical weight 0.0041, but its 31-fold preference outweighed a clear side view with physical weight 0.6804 after the side was suppressed to 3%. The bake now fades frontal preference with the fitted surface angle below the measured outer lower lip, reaching full release toward the chin. Registered eyes, brows, nose and lips retain coherent frontal ownership. Maximum support and coverage counters no longer include exposure preference. Those counters describe projection support on prepared images, which can contain generated cleanup; they are not proof that hidden skin was photographed.

Cheek exposure sampling now rejects transparent background pixels and requires substantial support in at least two cheek patches. It retains valid bright and dark photographed colors. In particular, warm skin with red values 250–252 must not be mistaken for clipped white highlights: excluding those pixels darkened the reference in the test capture. If the frontal reference lacks enough valid cheek pixels, the closest valid frontal view supplies the color reference; the selected filename is recorded.

The later jaw/neck trace separated two additional artifacts. General missing-head color and lower-skin continuation both faded through the same uncertain texels, producing an extra bright band at intermediate confidence. Resolved lower skin now blends once from its pre-completion appearance; unresolved regions, the neck cap, mouth and scalp retain their own fallback. That pre-completion appearance can contain registered cleanup estimates and must not be described as purely measured skin.

The prominent side-jaw stripe was different: neighboring points with strong photographic support abruptly preferred differently lit cameras as their fitted normals changed. A bounded lighting-field experiment reduced held-out camera disagreement but darkened the rendered side/neck too much, so it was rejected. The implemented correction instead smooths normalized camera ownership over actual surface edges, then applies only the interpolated change to the original texel weights. It preserves rejected-view masks, retreats if masking removes too much probability, and changes only the low-frequency blend. Central features, cleanup regions, separate parts and the shared scalp classification are protected. Physical support, fallback thresholds and the original fine-detail winner stay unchanged. A second projection visits only changed texels; no camera RGB gain is fitted. The current settings use six positive half-steps with a 4 mm edge scale at the estimated model scale; this is a decaying local influence, not a hard 4 mm radius or calibrated physical measurement.

## Current pipeline

1. Preserve the imported video and timestamped segmented captures. Recover higher resolution detail from nearby native video timestamps only after matching it to the registered frame. Retain browser-decoded low-frequency color to avoid changing skin tone through decoder/HDR differences.
2. Recover overlapping camera poses. Honor supplied intrinsics, including when caches already exist. Reserve facial views to check both triangulated landmarks and semantic locations on the final rendered skin. Select identity-fit regularization using a separate inner split and reject unsafe deformations. Absolute physical scale remains estimated.
3. Measure foreground sharpness and clipped exposure at a normalized crop size. Select clear frames near the desired camera angles rather than choosing by angle alone. Keep semantically annotated reference views.
4. Ask **gpt-6-astra** to annotate actual visible structures: hair masks/flow, opaque glasses ribbons, ear outlines and ear landmarks. These are fallible observations, not ground truth. Coordinates are tied to the exact image crop and native resolution.
5. Triangulate each ear landmark from independent rays with a baseline requirement, robust outlier rejection and a maximum reprojection error of 5 registered-image pixels. After the hair envelope and head prior are fitted, fit a bounded local transform to the whole ear, then regularize it with as-rigid-as-possible deformation while pinning the facial cage, observed face and distant surface. A geodesic attachment region lets the surrounding skull adapt; side-hair bindings are rebuilt afterward. Back off if triangle orientation, area or non-adjacent crossing checks fail. Refine ear ownership against visible photographed outlines and register ear-only texture samples to the fitted surface. Retain template inner-ear folds where the photographs do not establish detail.
6. Fit the visible hair envelope from silhouettes with a post-fit crown-height bound, pinned ear regions and a smooth attachment margin. Trace exterior locks along fused photographic orientation fields. Bind roots and curve stations to the surface so facial edits cannot detach strands. Individual fibers and unseen roots remain estimates.
7. Bake one coherent atlas. A photographed ear may only color its own ear surface; locally fill its former projection on the skull and label that appearance estimated. Preserve separate eyeball material. Clean opaque accessory ribbons before projecting skin. With AI completion enabled, request glasses-free estimates for up to three source angles, register them to unchanged source features, color-match them and feather only the affected region. Reject misaligned edits. Accepted cleanup references own those visible surface regions so competing views cannot reintroduce tinted lenses or rims. Restore enclosed segmentation holes only inside verified cleanup regions; retain background connected to the silhouette. Use strong, visible photographed hair evidence to constrain fallback material; an occluding ear is not evidence of bare scalp. Continue photographic color over the connected lower-head mesh into low-confidence regions, and give the closed neck section an explicitly estimated skin material.
8. Add separate polished acetate frames, clear lens surfaces and metal hinges. Their broad reflection field is independent of the captured skin lighting. The lenses do not refract already lens-distorted photographic eyes a second time.
9. Validate topology, finite values, facial invariance and matching geometry/texture hashes before publication. Full builds and existing-capture refinement write into separate staging directories. Mesh, atlas, textures, cage, bindings and geometric audit files form an immutable bundle; one atomic release pointer makes it current. Asset requests, Newton sessions and saved editable sessions pin that release. Failed work leaves the accepted bundle available, and workers are joined before failure is reported. Previous releases remain readable for saved sessions and in-flight requests. Full builds also retain a capture-specific pre-ear surface with its topology and regularization, so subsequent detail rebuilds do not reuse a baseline from an incompatible template fit. Inspect front, both profiles, back and bottom, with glasses hidden as well as visible, then exercise deformation and export.

`head-semantics.json`, `ear-measurements.json`, `frame-evidence.json`, `eyewear-mask-audit.json` and the mesh statistics retain the evidence. Missing or inconsistent observations should stay labeled estimated or cause a quality failure; they must not silently become measured anatomy.

## What the research actually supports

**Poisson surface reconstruction** is probably the method meant by “Poisson redistribution.” It reconstructs a surface from oriented 3D points. It does not establish camera calibration, recover depth from images, remove glasses, disentangle lighting or create a facial rig. It can help after reliable depth/normal estimation. Feeding Gaussian centers into it here would confuse volumetric blobs with surface samples. [Original authors' project](https://hhoppe.com/proj/poissonrecon/), [screened Poisson with envelope constraints](https://gfx.cs.princeton.edu/pubs/Kazhdan_2020_PSR/index.php).

**TRELLIS.2** uses sparse volumetric O-Voxel representations and generative models to produce geometry and material attributes. A useful design lesson is to keep geometry and appearance in a consistent 3D representation, including difficult thin structures. Generation is a prior; it does not prove that a synthesized hidden ear or hairstyle is this person's. The official implementation requires Linux/NVIDIA CUDA and associated extensions, so it does not fit this Mac's native toolchain. We have not installed it or benchmarked it against this capture. [Official repository](https://github.com/microsoft/TRELLIS.2), [paper](https://arxiv.org/abs/2512.14692).

**Meshy** publicly documents multiple images of the same object and PBR texture output. Those are product capabilities, not a published description of its internal training or reconstruction architecture. The transferable lesson is consistent multi-angle inputs and separate material attributes. There is no evidence here that our system exceeds Meshy, and no Meshy API request has been made. [Official multi-image API](https://docs.meshy.ai/en/api/multi-image-to-3d).

**GaussianAvatars** is more directly relevant to a deformable head: it binds appearance primitives to mesh triangles and jointly optimizes avatar geometry and appearance. The principle supports retaining a coherent surface coordinate system through facial animation. Its released software/assets are not an eligible drop-in for this repository's commercial-license and Mac constraints. No code or model assets have been copied. [Authors' project](https://shenhanqian.github.io/gaussian-avatars), [official code and license](https://github.com/ShenhanQian/GaussianAvatars).

**Astra** accepts image inputs and returns analysis/structured annotations. It is not an image-output model or a calibrated metrology tool. Here it proposes semantic observations; camera geometry and source-pixel checks constrain their use. Skin hidden by an opaque frame is still inferred. Raster cleanup uses the separate image-generation model, with explicit registration and a bounded composite; it is not an Astra image output. [Official model documentation](https://developers.openai.com/api/docs/models/gpt-6-astra).

**GNM**, Google's statistical head model, is a possible alternative identity prior with a permissive implementation. A local NumPy landmark-fit experiment used the already available assets, but did not establish better rendered geometry, rig compatibility or closed topology. It was not adopted into the production path. A sparse fitting score alone is insufficient evidence for changing the head representation. [Official repository](https://github.com/google/gnm), [paper](https://arxiv.org/abs/2607.23687).

**Multiview texturing** requires its own alignment and seam controls after geometry reconstruction. Waechter et al. describe view selection and global/local color adjustments; these address a different problem from Poisson surface reconstruction. This pass fixes demonstrated ownership and blending discontinuities in our implementation; it does not implement that paper's optimization or claim equivalent results. [Authors' paper](https://download.mmag.hrz.tu-darmstadt.de/media/FB20/GCC/paper/Waechter-2014-LTB.pdf).

## Next research stage, not yet implemented

The promising head-specific approach is a shared evidence model with separate ownership for skin, ears, eyeballs, eyelids, brows, beard, hair and eyewear. Jointly refine camera calibration, neutral surface, expressions and materials while explaining all selected frames. Each region should carry measured coverage and uncertainty. An accessory should occlude skin during fitting rather than becoming skin color. Hair should have a constrained outer volume plus surface-bound guide curves; dense strands add detail after that volume agrees with the photographs.

Use robust landmark, silhouette, multiview feature and photometric losses with smoothness, thickness and collision constraints. Estimate illumination/exposure separately from base color before offering relighting. Use learned models only to initialize or fill explicitly unobserved regions. Optimize the compact head representation locally using CPU/MPS-compatible operations; evaluate heavier hosted alternatives separately before making dependency or account commitments.

A benchmark should include several consenting identities, skin tones, hair types, glasses/no-glasses, both profiles, back and upward/downward views. Hold out entire camera angles and expressions. Compare silhouette error, landmark error, identity preservation, texture seams, accessory residue, hair boundary error, material response, rig stability and latency. Test calibrated synthetic cameras against their stored focal ground truth. Passing tests on one phone orbit does not establish generalization.

The current scan covers about 233 degrees, not a full orbit. Its unseen regions, glasses-hidden skin and insufficiently resolved eye detail cannot be recovered faithfully from missing evidence. “Works on any video” should mean robust handling and honest quality feedback; hyperrealistic identity recovery requires enough sharp, overlapping, well-lit observations. The current implementation still uses a generic facial/inner-ear prior and captured illumination, and has not reached the proposed joint inverse-rendering stage.

## Validation of the current capture

- The current preview contains the selected identity fit, protected ear regions, refined ear step and corrected temple lighting. Geometry, textures, physics cage and bindings were checked and published together. The previous preview remains in a timestamped `before-surface-fit-*` directory inside the capture. Older reconstruction backups remain intact.
- The complete current-capture benchmark used 51 saved frames, 40 recovered cameras, existing native video detail and cached AI annotations/references. Network requests were disabled. It selected regularization 0.01, with a maximum initial template change of 2.01 mm relative to 0.025 at the estimated scale. The complete final surface scored 2.85 px median and 11.99 px at the 95th percentile across 47 semantic locations in five reserved facial views. The accepted limits remain 4 and 12 px. The tail is close to the limit; this is not proof of accurate unseen anatomy.
- A separate 55-frame capture registered 54 cameras with cloud AI disabled. Regularization 0.005 passed after the stronger candidate failed a surface-shape gate. Eleven reserved views scored 1.89 px median and 7.96 px at the 95th percentile on the final surface. Missing photographic coverage within the measured facial outline was 1.3%. Its 85-degree span and absent rear observations still limit the reconstruction; it is not an independent identity benchmark.
- The calibrated synthetic fixture retained all 40 cameras at the supplied 1250-pixel focal length. Its eight reserved views scored 2.51 px median and 9.82 px at the 95th percentile on the rendered surface; missing facial coverage was 2.8%. These surface metrics are distinct from the smaller errors measured on the triangulated control cage.
- The three full cached-input runs took approximately 94, 61 and 49 seconds while running concurrently. Those durations exclude fresh frame extraction, camera recovery and cloud generation. They are not upload-to-finished-model latency promises.
- Ear fitting used a 0.9921875 step instead of the coarse 0.75 fallback. The maximum residuals to the triangulated anchors are 2.22 and 1.46 mm, with no reversed triangles or new detected non-adjacent intersections. Those anchors are fallible annotations and the physical scale is estimated. All protected facial/physics-active vertices remained unchanged during this ear refinement. Repeating detail reconstruction from the saved pre-ear surface produced exactly identical float32 vertex positions.
- A stale baseline is rejected even when its capture hash, regularization and topology match: its protected facial vertices must also match the installed mesh, and that mesh must match the saved physics cage. This guards against a failed prior build leaving incompatible artifacts.
- The three edited references passed registration with 70, 287 and 428 feature inliers, at median alignment errors of about 1.20, 0.56 and 0.51 native-image pixels. The image-generation tool made the edits; Astra supplied semantic annotations. The latest bake matched illumination on 228,529 cleanup texels, without changing geometry or measured coverage.
- Passed 82 Python tests, with one additional accelerator integration test skipped without an explicit capture. After Akash's concurrent repository update, synchronized the declared npm dependencies, passed all 69 JavaScript tests and the production build. The imported webcam update initially failed the existing stale-sample regression; contact tracking now expires at 350 ms while the last visible hand drawing can remain for one second, preserving the display improvement without allowing a reacquisition jump to become a punch.
- Browser checks covered the front, both matched profiles with glasses hidden, underside closure, jaw opening, held impact and release. The updated local preview uses 38,630 triangles and initializes the Newton rig. GLB accessory round-trip remains covered by automated tests. The bottom is closed. A subsequent connected-surface color pass removed the demonstrated under-jaw wedges and gray patch; some oblique photographic transitions remain.
- Accelerator compatibility now hashes the parsed function rather than formatting-sensitive source text. Formatting and comments retain compatibility; changed computation still disables the replacement until it is ported and re-pinned. A regression distinguishes harmless formatting from changed operator grouping.
- The first connected-surface material publication preserves every geometry coordinate, accessory, physics cage and binding. Its `before-surface-material-20260919-112445` backup contains the preceding appearance. That bake used 901,556 donor texels to anchor 1,586 vertices and solve 6,271 unsupported vertices; 667,912 lower-head texels received majority estimated color, without changing photographic coverage. All confident/protected texels stayed exact in the continuation stage. A post-save check compared 119,424 sampled lower-surface texels against the final PNG and verified matching mesh/texture hashes. The 55-frame capture also completed with no unresolved target texels, and all 1,374,594 checked lower-surface texels matched the saved atlas. These are the same identity, not a population benchmark.
- Material-only bakes took about 76 seconds for the current capture and 42 seconds for the 55-frame capture with cached source assets. That bake has 4,608 bounded eye-visibility rays; eyes remain generated. Browser inspection checked the repaired bottom, source-matched profiles, separate glasses and jaw deformation after publication.
- The subsequent jaw-view update is published with the preceding appearance in `before-surface-material-20260919-120256`. A traced grazing-front patch now uses the clearly visible side photograph: its color changed from RGB (0.604, 0.489, 0.421) to (0.433, 0.333, 0.281), while unpreferred projection support is 0.686. Upper facial features retain their frontal ownership. The new lower continuation uses 944,476 donor texels, 1,669 anchored vertices and 6,188 solved vertices, with no unresolved target vertices. Geometry and physics files remain unchanged, and 119,424 sampled final lower-surface texels match the saved continuation exactly.
- An opt-in real-capture integration check ran the final texture projector with one and four workers in isolated copies, with network/source writes prohibited. Both texture PNGs, atlas mappings and evidence data were identical; input geometry was unchanged and relevant Python computation stayed unchanged throughout. The cached material runs took 207.1 and 66.8 seconds on this machine. These runs exclude camera recovery and cloud generation. The published PNGs match both verified outputs exactly. The report records code and asset hashes in `.local/view-blending-verification.json`.
- Transactional publication passed a complete cached capture build in 98.85 seconds in an isolated capture, retaining the same 2.85/11.99 px final-surface check. A second run deliberately failed grooming while a delayed texture worker wrote partial outputs. All 11 accepted artifact hashes and all 11 legacy artifact hashes remained unchanged; the worker finished before terminal failure, and staging was removed. Fault-injection tests also cover pointer-write failure and interruption immediately after pointer replacement. Files are flushed before publication, but full power-loss durability has not been tested.
- The live capture was adopted into release `94dd587f6ac14d39b07ae9ebb7a9fb0e` without changing geometry, texture pixels, cage or bindings. Only atlas asset URLs gained the release identifier. The previous root files remain intact. Browser loading and jaw controls initialize the expected Newton cage; the server verifies a fingerprint of cage positions and triangles against the client. Generation-aware API tests preserve model availability even after a failed reconstruction attempt.
- Hair refits now transfer their persisted displacement into the independent pre-ear baseline. This keeps a later ear-detail rebuild from undoing an accepted hair envelope. Incompatible baselines and movement of protected facial/ear vertices are rejected. Current publication checks passed 110 Python tests with one optional accelerator integration skip, plus two Newton session tests. The JavaScript suite and production build also pass; detailed run logs are under `.local/head-publication-*`.
- The latest material revision is `f565f1bb3a124733ba9f51a2e080a1e5`; `e432cf85ac414105ab7ec419fa170d0f` remains available. Every geometry coordinate, accessory, cage, binding, ear baseline and surface-fit validation is unchanged. Camera-ownership smoothing modifies 1,070,370 low-frequency skin texels while retaining the original physical support and fine-detail winner. The traced side-jaw pair's RGB distance falls from 0.2364 to 0.1476; its mean changes by roughly 0.006 per channel. This measures one diagnosed seam, not overall likeness. The single-completion correction separately removes the intermediate-confidence bright belt. Sampled mouth, neck-cap, full-scalp and cleanup regions remain exact in the ownership comparison.
- The final cached material bake is byte-identical with one and four workers, including the PNGs and atlas metadata. Those runs took 78.9 and 36.7 seconds; other validation jobs overlapped the first run. Source writes and network access were prohibited. The published PNG matches both outputs. A second 55-frame capture completes the new bake, but matched-camera inspection still shows baked eyewear and substantial inferred ear/rear/neck appearance when AI cleanup/semantic annotations are absent. That capture does **not** pass a hyperrealism criterion and was not published over the user's accepted model. The review tool now handles captures without hair annotations, native-detail caches or timestamps, using saved-frame labels rather than invented times.
- The final check passed 130 Python tests with one optional accelerator integration skip, plus the production web build. Regression tests cover confidence-ramp fallback overlap, exact zero-diffusion/zero-blend behavior, camera rejection masks, pinned/unsupported vertices, disconnected surfaces and central-feature exclusion. Detailed reports are in `.local/jaw-seam-final-verification.json`, `.local/jaw-seam-regression-final.json` and `.local/jaw-seam-publication.json`.
- Remaining realism limits include generated eye appearance and lighting, inner-ear folds inherited from the template, residual oblique/temple texture transitions, and incomplete underside/rear observations. The source eye crops do not isolate reliable sclera donors: apparent samples mix blurred eyelid and skin color, so no automatic correction was fitted to those contaminated samples. Neither hyperrealism on arbitrary videos nor superiority to Meshy/TRELLIS has been established.

### Temple classification correction

The next material release, `132c44ce10c449a3a2931f92b7549b50`, preserves the geometry, glasses, physics cage, binding and ear baseline from `f565f1bb3a124733ba9f51a2e080a1e5`. Astra traced the dark diagonal temple streak to a classification error: `facing**8` was being used both to prefer sharp texture and to decide whether semantic skin evidence could override the hair prior. At the traced point, a clearly visible source-skin observation had semantic support 0.3445 but texture preference only 0.0583. The prior therefore supplied 52% inferred hair.

Scalp classification now uses separate semantic visibility, with a smooth confidence ramp and a required useful individual view. Many weak grazing observations cannot collectively erase an unseen-region prior. Original source alpha, opaque glasses and ear occlusion remain authoritative; filled alpha holes and opaque frame pixels cannot vote as observed skin. Both camera blending and missing-material completion use the same classifier.

The matched -68-degree preview no longer shows the diagnosed streak. Its sampled RGB changed from (0.517, 0.410, 0.340) to (0.833, 0.623, 0.516); this removes the erroneous hair contribution, not a claim of recovered skin albedo. Two real-hair controls and an unsupported-region control stayed bit-identical. Physical support, captured color accumulators, fine-detail ownership and cleanup coverage stayed identical across 24,289 diagnostic samples. The serial and four-worker bakes produced identical PNGs in 48.8 and 32.4 seconds, with network access/source writes prohibited. All 78 relevant Python tests passed. Reports are `.local/temple-semantic-verification.json`, `.local/temple-semantic-regression.json` and `.local/temple-semantic-publication.json`. Existing jagged appearance behind the ear and template/eye limitations still require work; this is not a hyperrealism pass.

### Nape geometry and repeatable ear refinement

Release `13995bae0d4e4b9f8b1378281af38cc0` replaces the shelf-like lower hair envelope with a smooth estimated nape transition. Astra traced the fold to the hair fitter's short activation ramp: a large posterior hair displacement returned abruptly to the unexpanded neck. This was not a detected self-intersection, so crossing checks alone could not identify it.

`hair_transition.py` continues that displacement along mesh edges while pinning the full hair envelope, measured face, neck cut and ear support. Artificial cap diagonals do not shortcut the solve. A biharmonic continuation is bounded by triangle orientation, area and intersection checks; unsupported components remain unchanged. It is an estimated transition, not recovered hidden neck anatomy.

The first candidate passed geometry tests but a later ear rebuild moved the attachment by up to 0.226 mm. ARAP rotations depend on two edge rings beyond the nominal 35 mm ear support. Pinning that additional stencil removed the coupling. The final complete build changes 434 posterior vertices, preserves the full envelope and protected face/ear coordinates exactly, and passes a geometry-only detail rebuild with **identical float32 positions**. A regression exercises the interaction with the actual ear regularizer. No reversed triangles or new detected crossings were introduced.

In the affected -68-degree source-view region, median outline distance remains 15 pixels and the 95th percentile improves from 35.34 to 25.50 pixels; mask disagreement decreases from 4,957 to 4,328 pixels. The 149-degree region improves slightly. Those regions exclude the source's artificial lower crop, and are diagnostic comparisons used during development, not withheld quality estimates. Texture artifacts behind the ear still remain.

The cached complete builds took 48.3 seconds for the 51-frame capture and 29.1 seconds for the 55-frame capture while running concurrently. Network requests and writes to the source captures were prohibited. Both retain their previous final-face checks: 2.85/11.99 pixels and 1.89/7.96 pixels median/95th percentile. These captures are the same identity. The second has limited coverage and absent AI cleanup; passing its structural gates does not establish realistic appearance.

The published bundle contains the freshly baked atlas, regenerated strand bindings, physics binding and updated independent ear baseline. The cage, measured face and ear measurements remain exact. All six browser-loaded assets match their release hashes. Browser inspection covered both affected profiles, the rear, underside closure and jaw motion; the main preview initializes Newton and is left neutral. The previous generation remains available. Reports are `.local/hair-transition-publication.json`, `.local/hair-transition-final-detail-rebuild-check.json`, `.local/hair-transition-api-verification.json` and `.local/posterior-continuation-isolated-signed-silhouettes.json`. All 67 relevant Python tests passed.

An independent [MapAnything feasibility experiment](docs/HEAD_DEPTH_EXPERIMENT.md) ran locally on MPS with known synthetic cameras. Raw geometry and oracle-calibrated outputs did not reach useful facial accuracy, so learned depth was not added to the published reconstruction. The broad hyperrealism goal remains unproven: unseen anatomy, generic inner ears and eyes, captured illumination and residual hair/skin ownership errors require further work.

### Oblique skin evidence and hair-boundary regression

Material release `50b6301c2b3247e7b67b80ff83cb7d2f` retains all geometry, glasses, groom, cage, binding and independent ear baseline from `e4d4c9b3b65744acbbb3b6680027042c`. It removes the large false hair patch on the skin behind the left ear. Astra traced a clearly visible skin observation whose cubic angular score fell just below the semantic trust threshold; missing-material completion consequently supplied nearly all hair color there.

Semantic support now uses projected area with a separate grazing fade where the class annotation supports that relaxation. Skin observations near a coarse hair polygon or original-alpha edge retain their previous conservative angular score. The boundary width scales with the annotated crop. Original alpha, opaque glasses, source-ear occlusion and depth visibility remain authoritative; photographic RGB selection and high-frequency detail are unchanged.

Two intermediate candidates were rejected. Applying the relaxed score everywhere created a small pink crown strip from pixels only 1–3 source pixels outside the coarse hair polygon. Removing all skin evidence in a boundary deadband then made a previously correct skin control fall back to hair. The final bounded upgrade retains existing conservative boundary evidence and qualifies only its additional oblique support. It removes the crown regression while correcting the demonstrated post-ear skin patch.

Across 24,024 matched diagnostic surface samples, geometry, normals, part ownership, physical photo support, captured-color accumulators, fine-detail ownership and cleanup coverage remain exact. Three diagnosed patch controls change from hair to skin; real-hair, adjacent-skin and occluder controls remain unchanged. One representative RGB changes from (30, 21, 18) to (185, 128, 106). That replacement includes estimated skin continuation; it is not recovered intrinsic albedo or new photographic detail.

The final serial and four-worker bakes are byte-identical, taking 38.75 and 24.69 seconds with source writes and network prohibited. All six browser assets match the release hashes. There were 67 passing Python tests and one optional accelerator integration skip. Browser inspection covered both profiles, the crown/rear and front with separate glasses; the main preview initializes Newton and returns to neutral after a jaw-motion smoke test. Residual ear attachment seams, a smaller posterior boundary mark, inferred eyes and incomplete observations remain unresolved. This is not a hyperrealism pass.

Reports are `.local/postear-supported-regression.json`, `.local/postear-supported-verification.json`, `.local/postear-supported-publication.json` and `.local/postear-supported-api-verification.json`. Rejected candidates remain private diagnostic files and were never published.

The separate [SAM2.1 native-frame experiment](docs/HEAD_MASK_EXPERIMENT.md) improves dense hair boundary proposals in seven views, but rejects substantial real sparse-fade candidates even when all three proposals agree. It is not enabled in the production material or silhouette fitter. Its confidence/unknown representation and source-alpha limitations need validation before adoption.

### Photographic priority behind source ears

Material release `4b858b2acfa04805b5cb7f77bc35df5e` preserves the mesh, groom, accessory geometry, facial cage, binding and independent ear baseline from `50b6301c2b3247e7b67b80ff83cb7d2f`. It includes the current short-hair fallback and source-ear masking code. The direct ear-priority correction prevents masked ear observations from forcing inferred hair onto skin supported by an unmasked photograph. A diagnosed rear-side texel had strong photographic support (0.601) but the old hidden-scalp stage still applied 95.7% inferred hair. `ear_appearance.hidden_scalp_completion` now fades that fallback to zero as useful unmasked support reaches 0.12. This does not create additional coverage or alter the ear geometry.

An initial exact replay isolated that priority bug while preserving physical support and protected controls. During subsequent work, the source-ear occluder widened to 7 by 7 pixels and the rear short-hair material changed in the shared workspace. The first candidate failed the publication hash check and was not installed. The final candidate was rebuilt and verified against the then-current code: serial and four-worker PNGs are byte-identical (85.11 and 51.95 seconds). All six browser assets match the immutable release hashes, and 57 targeted Python tests passed. Reports: `.local/ear-priority-current-verification.json`, `.local/ear-priority-publication.json`, `.local/ear-priority-api-verification.json`.

The remaining crescent is not wholly the same bug. Some affected surface points have no usable unmasked photograph. A private mesh-geodesic fill reduces part of it, but leaves visibly discontinuous fragments and is not enabled in the production bake. Its exact integration check preserved all 2,782,725 texels with support at least 0.12. An apparently changed high-confidence control actually had zero support under the newer dilated source-ear mask; its old trace was stale. The diagnostic is `.local/ear-occlusion-invariant/invariant.json`. Neither a successful unit test nor a small ear-anchor residual establishes a clean rendered ear attachment.

To prevent another helper from escaping the source-stability check, the verifier now follows local Python imports, including imports inside functions and relative package helpers, without executing them. It detects helper changes while ignoring formatting and unrelated files. Three focused regression tests cover that behavior.

The bake now also snapshots supported part-0 prepared color before generic completion, excluding cleanup and the neck cap. After all completion stages it compares the actual PNG-quantized RGB, with final mouth/interior exclusions. A mismatch writes bounded diagnostics and fails before the appearance PNG or a new release is produced. This guards preservation of the prepared blend; it does not certify that the source registration or generated cleanup was correct. The production guard preserved 1,843,633 texels in the real cached capture. Its negative control restores the former hidden-hair overwrite: 10,941 protected texels changed, the diagnostic was saved, and no appearance PNG was written. Both stages used the same saved release. The process retained its imported code while a concurrent task updated photo registration on disk; this is a gate regression experiment, not verification for publishing those newer registration changes. The report is `.local/production-supported-color-guard.json`. All 69 targeted Python tests passed after the guard and bounded-fill helper changes.

A stricter private ear-fill experiment evaluates conservative surface paths at individual texels and fades support over the final 4 mm of a 20 mm radius. Its protected-color invariant passes, but the rendered attachment remains visibly incomplete, so it is still not hooked into production. These development comparisons do not validate anatomy or generalization. Residual attachment seams and uncertain unobserved appearance remain open work; this release does not establish hyperrealistic results or generalization to arbitrary videos.

### Native extraction edge color and matte validation

Astra identified a source-detail bug independent of the remaining ear-attachment seam: resampling zero RGB outside the registered alpha and subtracting an unmasked native blur manufactured dark hair borders. The shared `detail_color.fuse_native_detail` now normalizes both registered resampling and smooth color correction by foreground support. New standard and accelerated extractions record version 3 with per-frame audits; accepted version-2 image caches remain unchanged because downstream generated cleanup and semantic caches bind their bytes. Empty foreground frames cannot pass a NaN match score. Downscaling has separate coverage: a vanished alpha hole cannot incorrectly qualify the old color calculation as safe, and area resampling retains sparse registered color support.

There are 31 passing focused tests plus 13 accelerator tests, including complete byte-identical manifests and all 51 PNGs from the real recording. A private full-texture comparison preserves geometry, alpha, opaque source interiors and the production supported-color gate. It also exposes a dependency: re-registering the same generated glasses-cleanup photograph changes its alignment when the input RGB changes. That comparison must not be described as an isolated color-only test. The published input cache was not migrated. Full provenance and caveats are in [native-detail validation](docs/HEAD_NATIVE_DETAIL.md).

A separate [native outer-matte experiment](docs/HEAD_MASK_EXPERIMENT.md#native-outer-matte-experiment) returns to the original video and plausibly restores three diagnosed wisps removed by the registered alpha. Fourteen proposals across seven views preserve known alpha but initially change some known-foreground colors by up to 47 channel levels. Private corrected proposals restore original RGB outside the uncertain band exactly. Matting remains disabled in production until temporal, color and uncertain-evidence handling are validated. Neither the new color-fusion path nor this experiment establishes realistic individual fibers, clean ear attachments, intrinsic skin color or generalization to arbitrary videos.

### Ear occlusion and cleanup ownership

A connected head-surface continuation now addresses the dark crescent left where source ears correctly occlude the surrounding head. Temporary categorical clipping reaches head-owned portions of mixed triangles without changing the model mesh or borrowing color through an ear. A second traced bug allowed source-ear inpainting to masquerade as verified glasses cleanup; RGB and ownership now share the same per-view exclusions, and valid cleanup suppresses later inferred hair. [Ear appearance validation](docs/HEAD_EAR_APPEARANCE.md) records the implementation and actual rendered comparisons. Small unsupported ear-owned attachment patches remain. Fresh-process tracing also captured sparse, large errors in the ear affine projection. Explicit row arithmetic now avoids that path, and independent serial/four-worker production bakes pass exact texture and metadata comparisons. The targeted suite passes 96 tests, with one optional test skipped; the real-capture check was run separately. Generation `8488bd76981e4320bcde74d59249456d` publishes the verified appearance, including the concurrently developed estimated crown material. Geometry, UV correspondence, groom, accessory geometry and rig files remain unchanged; all six served browser assets match the release manifest. Residual ear-owned flaps and estimated eyes remain visible.

### Source-constrained ear outlines and stage snapshots

Generation `3fd53b91e39344e8bf81aaacfb7b9162` reduces the remaining outer ear flaps using reliable visible source arcs, with fixed face/cage/ear anchors, bidirectional contour gates and independent pre-ear surface checks. The maximum change is 7.075 mm. Three approximate held-out arcs from the same recording improve in overall error; the right-side check covers only a short upper rim. Existing photographic hair bindings and colors are retained while 172 roots and 3,347 curve stations follow the edited surface. Attachment appearance seams and template inner folds remain.

A later hair edit had made nonlinear ear replay drift by up to 2.419 mm. Separate hashed post-ear contour-stage snapshots now preserve the accepted input and prevent cumulative refinement. Full builds, detail rebuilds and hair-refit baseline updates share this stage contract. The old visibility-only material classifier now retains anatomical ear identity. The corrected detail rebuild reproduces the candidate's geometry and groom exactly; fresh-process serial/four-worker bakes match PNGs and atlas metadata, and all six served assets match the release. There are 124 passing targeted Python tests and 10 hair-renderer tests. A fresh live Newton session was not validated in this pass. Full evidence and limits are in [ear contour validation](docs/HEAD_EAR_CONTOURS.md). The broad hyperrealism goal remains unproven.

### Unmasked source review and measured eyewear persistence

The review now shows matched unmasked video frames by default, with an explicit
cutout toggle. The old mask removed a real far lens, so absence in that cutout
cannot serve as an accessory-visibility observation. Full builds and detail
rebuilds now share the same measured eyewear fitter; a private complete rebuild
preserves head/hair/skin/physics exactly while retaining 51 measured rim widths.
Sampled renderer checks also reveal existing arm intersections, so the new
glasses proposals remain private. Astra's exact atlas trace identifies false
predicted-ear donors. An experimental bake hook now reduces their influence
when two independent annotated views contradict them. Fresh serial/threaded
bakes agree exactly, but the resulting darker estimate does not fix the visible
attachment seam, so the hook stays off by default and is not published. See
[source evidence and validation](docs/HEAD_SOURCE_OBSERVATIONS.md) and the
[ear donor experiment](docs/HEAD_EAR_DONORS.md).

### Continuous estimated head/ear attachment

Astra's native-frame and exact-atlas checks identified a separate color-field
discontinuity at the attachment: the head and ear fills assigned different
estimated colors to adjacent points on the same facet. One continuous local
surface field now joins unsupported attachment samples while preserving the
measured face, reliable skin/hair, cleanup, eyes, mouth and cap. Excluded eye/cap
faces cannot supply anchors through shared vertices.

The corrected implementation passes 44 focused tests and fresh serial/threaded
bakes with exact PNG/atlas agreement. Browser and independent matched-angle
comparisons show cleaner upper attachments and lower lobes. Generation
`41fa09628c104090af7ffb8253fbce56` is the accepted local texture-only update;
geometry, accessories and rig remain exact. The inferred tones, template inner
ears and other capture/appearance limitations remain. See
[attachment evidence and release checks](docs/HEAD_ATTACHMENT_CONTINUATION.md).

## Local use

```sh
PYTHONPATH=. .venv/bin/python scripts/rebuild_head_details.py .local/face-captures/CAPTURE_ID
PYTHONPATH=. .venv/bin/python scripts/prepare_hair_review.py .local/face-captures/CAPTURE_ID
PYTHONPATH=. .venv/bin/python -m unittest tests.head_semantics_test tests.head_template_test tests.hair_eyewear_test
```

Serial/threaded texture equivalence can be checked without publishing or changing the capture:

```sh
PYTHONPATH=. .venv/bin/python scripts/verify_texture_bake.py .local/face-captures/CAPTURE_ID --report .local/texture-verification.json
```

This runs one-worker and four-worker bakes in separate fresh Python interpreters against cached inputs, compares exact PNG bytes and atlas metadata, rejects source writes/network access in each worker, and checks that the relevant code did not change during the run. Fresh processes prevent shared in-memory state from concealing repeatability defects.

Accepted model artifacts are resolved with `head_artifacts.published_folder(capture)`. New releases live under `CAPTURE/.model-generations/`; `model-release.json` identifies the current bundle. Root images and AI caches remain reusable inputs. Legacy root model files are retained for old sessions and are not the latest model after a new release. Use the current build/refinement commands above; the old Gaussian/legacy surface writers refuse to overwrite a generation-based capture.

The matched review runs at `/hair-review.html` on the development server. Inspect the actual source and rendered model together; a successful rebuild is not sufficient visual validation.
