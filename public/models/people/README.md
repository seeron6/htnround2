# Named head models

These are the named GLB exports intended for finding and loading the models in the repository:

- `akash.glb` — Akash's local photographic full-head reconstruction, exported from the completed 41-view capture.
- `mark.glb` — Mark's Meshy head reconstruction from the named Mark capture.

Both files are self-contained GLB assets and are tracked in Git.
